import { AccessRoleDirective } from './access-role.directive';

describe('AccessRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessRoleDirective();
    expect(directive).toBeTruthy();
  });
});
